package Core.messaging;

import API.moduleInfo;

@moduleInfo(dependencies="",name="VK Message Shell",version=Core.version.CoreVersion,build=Core.version.CoreBuild)
public class msh {
	public String cmd(String what){
		return what;
	}
}
